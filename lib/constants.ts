/** Max substitution changes per team per match. */
export const MAX_MATCH_SUBS = 5;

/** Named substitutes available during a match (not the full squad). */
export const MATCH_BENCH_SIZE = 5;

/** Captain boost lasts this many sim ticks after call (~30s each). */
export const CAPTAIN_BOOST_TICKS = 8;
